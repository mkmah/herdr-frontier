---
"herdr-frontier": minor
---

The list cursor now walks category header rows like ordinary rows — a whole category can be selected as a unit, its summary (name, count, open, your-turn) paints in the detail pane, the four Confirmable verbs no-op on it, and `t` scopes the dependency tree to that category's whole graph.